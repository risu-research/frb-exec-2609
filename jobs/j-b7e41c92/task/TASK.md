# Task

Implement `decide(x, limit)` so that it returns `"ALLOW"` exactly when `x` is strictly less than `limit`; otherwise it must return `"DENY"`.

Make the repository pass its verifier. Preserve the requested behavior for all integer inputs represented by the verifier's declared domain.
